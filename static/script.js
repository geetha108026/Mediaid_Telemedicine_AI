const slides=Array.from(document.querySelectorAll('.slide'));let idx=0;function show(i){slides[idx].classList.remove('active');idx=i;slides[idx].classList.add('active');}
document.querySelectorAll('[data-next]').forEach(b=>b.addEventListener('click',()=>show(Math.min(idx+1,slides.length-1))));
document.querySelectorAll('[data-prev]').forEach(b=>b.addEventListener('click',()=>show(Math.max(idx-1,0))));
const btnDiag=document.getElementById('btn-diagnose');btnDiag&&btnDiag.addEventListener('click',async()=>{const symptoms=(document.getElementById('symptoms').value||'').trim();if(!symptoms){alert('Please describe your symptoms.');return;}const res=await fetch('/api/diagnose',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({symptoms})});const data=await res.json();document.getElementById('diag-summary').textContent=data.summary;document.getElementById('diag-disclaimer').textContent=data.disclaimer;document.getElementById('urgent-banner').hidden=!data.urgent;document.getElementById('diag-result').hidden=false;});
const btnMental=document.getElementById('btn-mental');btnMental&&btnMental.addEventListener('click',async()=>{const phq1=+document.getElementById('phq1').value;const phq2=+document.getElementById('phq2').value;const gad1=+document.getElementById('gad1').value;const gad2=+document.getElementById('gad2').value;const res=await fetch('/api/mental',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phq2:[phq1,phq2],gad2:[gad1,gad2]})});const data=await res.json();const lines=[];lines.push(`PHQ‑2 total: ${data.phq2_total} → ${data.phq2_result}`);lines.push(`GAD‑2 total: ${data.gad2_total} → ${data.gad2_result}`);lines.push(data.note);document.getElementById('mental-result').hidden=false;document.getElementById('mh-lines').innerHTML=lines.map(l=>`<p>${l}</p>`).join('');});
const btnBook=document.getElementById('btn-book');btnBook&&btnBook.addEventListener('click',async()=>{const name=(document.getElementById('name').value||'Patient').trim();const phone=(document.getElementById('phone').value||'').trim();const date=document.getElementById('date').value;const time=document.getElementById('time').value;if(!phone||!date||!time){alert('Please fill phone, date, and time.');return;}const res=await fetch('/api/book',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,phone,date,time})});const data=await res.json();document.getElementById('book-result').hidden=false;document.getElementById('book-msg').textContent=`${data.message} • ID: ${data.appointment_id}`;});
show(0);